// Set this to your deployed backend's URL when the frontend and backend are
// hosted separately (e.g. frontend on Vercel, backend on Render):
//
//   window.API_BASE_URL = "https://your-backend.onrender.com";
//
// Leave it as "" if the frontend is served by the same Python server as the
// backend (e.g. running locally with `python3 main.py` and opening
// http://localhost:8000/).
window.API_BASE_URL = "https://anime-video-backend.onrender.com";
